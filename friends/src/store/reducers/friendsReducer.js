const initialState = {
    fetching: false,
    friendsList: []
}

export default function friendsReducer(state=initialState, action) {
    switch(action.type) {
        default: 
            return state
    }
}